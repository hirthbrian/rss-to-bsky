BSKY_APP_PASSWORD=
BSKY_HANDLE=
# Optional. Defaults to /data/posted.db (the Docker volume path).
# For local `go run` testing, point this at a project-relative path, e.g. ./data/posted.db
DB_PATH=
RSS_URL=
